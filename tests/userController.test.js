import userService from './services/userService';
import { catchAsync } from '../utils/error';
import { Test } from 'supertest';

test("테스트 설명", () => {
    expect("검증 대상").toxxx("기대결과"); // toxxx는 Test Matcher
});

test('fetch a user', catchAsync(async(req,res) => {
    const { authorizationCode } = req.query;

    if
}))
export signin = catchAsync(async(req, res) => {

    const 

}
